name: release-plz

permissions:
  pull-requests: write
  contents: write

on:
  workflow_dispatch: {}
  push:
    branches:
      - main
      - release-plz

concurrency:
  group: release-plz

env:
  DRY_RUN: 0

jobs:
  release-plz:
    runs-on: ubuntu-latest
    steps:
      # release-plz pushes tags and the release branch via `git push`, which needs the scoped
      # FNOX_GH_TOKEN persisted in the git config.
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6 # zizmor: ignore[artipacked]
        with:
          fetch-depth: 0
          token: ${{ secrets.FNOX_GH_TOKEN }}
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libdbus-1-dev libudev-dev pkg-config
      - uses: jdx/mise-action@1648a7812b9aeae629881980618f079932869151 # v4
        with:
          experimental: true
          cache: false
      - run: mise trust --all
      - name: Install Claude Code CLI
        run: npm install -g @anthropic-ai/claude-code
      - run: mise run release-plz
        env:
          GITHUB_TOKEN: ${{ secrets.FNOX_GH_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
